name: auth-service
on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: write
  id-token: write
  security-events: write

jobs:
  ci:
    uses: ./.github/workflows/service-ci.yml
    with: { service: auth-service, language: go }
    secrets: inherit
